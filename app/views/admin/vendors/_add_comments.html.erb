<%= simple_form_for @vendor, url: create_comments_admin_vendors_path(id:@vendor.id) , method: :post, remote: true, html: {id:"add_comments"} do |f| %>
  
    <div id="form_message" class="two fields red" style="margin-left: 15px;display:none;" ></div>

    <div class="two fields">
      <%= f.input :content %>
    </div>
    
    <div class="field">
      <div class="actions ">
        <div class="ui button primary" id="commition" >提交</div>
        <div class="ui cancel button">取消</div>
      </div>
    </div>

<% end %>