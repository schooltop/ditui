$('#vendor_modal').remove();
$('body').append('<div class="ui modal" id="vendor_modal"><i class="close icon"></i><h2 class="ui dividing header">添加类型</h2><%= j(render 'new') %></div>');
$('#vendor_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
  $("#commition").click(function(){
    if( $("#vendor_name").val() !="" ){
      $("#new_vendor").submit(); 
    }else{
      $("#form_message").text("请完善信息！").show(200);
    }
  });
