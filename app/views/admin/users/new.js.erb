$('#user_modal').remove();
$('body').append('<div class="ui modal" id="user_modal"><i class="close icon"></i><h2 class="ui dividing header">添加类型</h2><%= j(render 'new') %></div>');
$('#user_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
  $("#commition").click(function(){
    if( $("#user_name").val() !="" ){
      $("#new_user").submit(); 
    }else{
      $("#form_message").text("请完善信息！").show(200);
    }
  });

  $('#user_id').change(function(){
  	  var id = $('#user_id').val()
  	  if (id != ""){
	  	  $.ajax({
	          url: "/admin/users/show_name",
	          type: "get",
	          data: {id: id},
	          success: function(data){
	            $("#show_name").html(data.name)
	          }
	        });
	      }
    })
