
<%= simple_form_for @user, url: save_roles_admin_users_path(id:@user.id) , method: :post, remote: true, html: {id:"add_roles"} do |f| %>
  
    <div id="form_message" class="two fields red" style="margin-left: 15px;display:none;" ></div>

    <% Role.all.each do |role|%>
	    <label class="checkbox">
	      <%= check_box_tag "user[role_ids][]",role.id,@user.role_ids.include?(role.id)%> <%= role.name %>
	    </label>
	<% end %>
	<br/>
    
    <div class="field">
      <div class="actions ">
        <div class="ui button primary" id="commition" >提交</div>
        <div class="ui cancel button">取消</div>
      </div>
    </div>

<% end %>


