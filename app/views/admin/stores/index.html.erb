<div class="ui grid">
  <div class="left floated fourteen wide column">
    <h2><a href="javascript:history.back()"><i class="blue reply icon"></i></a>新店管理</h2>
  </div>
</div>
<%= render 'search_form'  %>

<div class="ui blue segment">

    <div class="ui grid">
      <div class="left floated wide column">
        <%= link_to '新增store', new_admin_store_path, class: 'ui primary button', remote: true  %>
      </div>
    </div>
  <table class="ui selectable striped celled table">
    <thead>
    <tr >
      <th class="center aligned">ID</th>
      <th class="center aligned">封面</th>
      <th class="center aligned">名称</th>
      <th class="center aligned">address</th>
      <th class="center aligned">操作</th>
    </tr>
    </thead>
    <tbody>
    <%= render partial: 'list', collection: @stores, as: :store %>
    </tbody>
  </table>
  <span class="blue">
    <%= page_entries_info(@stores) %>
  </span>
  <%= paginate(@stores, :theme => 'admin')%>
</div>